INTERLIS 2.4;

MODEL GenericContextDimensionInvalid AT "https://example.invalid" VERSION "1" =
  DOMAIN
    Position (GENERIC) = COORD NUMERIC, NUMERIC, NUMERIC;
    Concrete = COORD 0 .. 100, 0 .. 100;
  CONTEXT default =
    Position = Concrete;
END GenericContextDimensionInvalid.
